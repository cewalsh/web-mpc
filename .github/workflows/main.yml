# This is a basic workflow to help you get started with Actions

name: CI2

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  run_tests:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
#     container: circleci/mongo:latest
#     container: cimg/node:current-browsers

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
#       steps:
      #install chrome
      - run: sudo apt-get install google-chrome-stable
      - run: whereis google-chrome
#       - run: chrome --version
      - uses: actions/checkout@v3
#       #checkout web-mpc
#       - run: git submodule init 
#       - run: git submodule update
      #checkout jiff
      #- run: cd web-mpc
      - run: git submodule init 
      - run: git submodule update
      #restore deps
      - name: Cache node modules
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: v1-npm-deps-${{ hashFiles('**/package-lock.json') }}
          restore-keys: v1-npm-de
      
      #install deps
      - run: npm install
      - run: cd jiff
      - run: npm install
      - run: cd ..
      #cache deps
      #not implemented
      
      #run tests
      - run: npm install --save selenium-webdriver chromedriver
      - run: ls
      - run: node test/selenium/small_test.js
      - run: npm run test
      
      #done
      - run: echo Done
